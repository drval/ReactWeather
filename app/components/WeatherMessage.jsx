var React = require('react');

var WeatherMessage = React.createClass({
    render: function () {
        return (
            <h3>It's 10C in Toliatty</h3>
        );
    }
});

module.exports = WeatherMessage;